import React from 'react';
import classes from './Header.css';

const header = (props) => {
    return (
        <header>
            <div>
                <div>Logo</div>
                <div>Naviation</div>
            </div>
        </header>
    );
};

export default header;