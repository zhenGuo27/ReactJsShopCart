import React from 'react';
import './Header.css';

const header = (props) => {
    return (
        <header>
            <div>
                <div>Logo</div>
                <div>Naviation</div>
            </div>
        </header>
    );
};

export default header;